node_modules
.env
runs/
quality-probe-runs/
role-experiment-runs/
*.log
.idempotency/
tasks/
# v6 §6: an operator's own persona set and forbidden-name list - real names,
# never committed. See PERSONAS.md.
.council/
Review/
# Packaged binaries from `npm run build:bin` / `build:appimage` - release artifacts, never committed.
dist/
