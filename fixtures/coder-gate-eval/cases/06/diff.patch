--- a/target.js
+++ b/target.js
@@ -1,4 +1,5 @@
 export function initials(name) {
-  let parts = name.trim().split(/\s+/);
+  // One letter per word, whitespace-separated.
+  const parts = name.trim().split(/\s+/);
   return parts.map(p => p[0].toUpperCase()).join('');
 }
