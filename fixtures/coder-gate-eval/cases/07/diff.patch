--- a/target.js
+++ b/target.js
@@ -1,5 +1,3 @@
 export function maxOf(xs) {
-  let best = -Infinity;
-  for (const x of xs) if (x > best) best = x;
-  return best;
+  return xs.reduce((best, x) => (x > best ? x : best), -Infinity);
 }
